---
import Base from "../layouts/Base.astro";
const year = new Date().getFullYear();
---

<Base>

  <!-- Fondo con grain estilo Waita -->
  <style>
    .noise {
      position: fixed;
      inset: 0;
      pointer-events: none;
      background-image: url("data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABAQMAAAAl21bKAAAAA1BMVEX///+nxBvIAAAACklEQVR42mOAAQAAAgAB9HFkWQAAAABJRU5ErkJggg==");
      opacity: 0.07;
      z-index: 50;
    }
  </style>

  <div class="noise"></div>

  <main class="min-h-screen flex items-center justify-center py-16 lg:py-28 bg-[#020617] text-slate-50 relative">

    <div class="w-full max-w-7xl mx-auto px-6 lg:px-12">

      <!-- HEADER estilo Waita -->
      <header class="flex items-center justify-between mb-12">
        <div class="flex items-center gap-3">
          <div
            class="h-10 w-10 rounded-xl border border-[#B79A29]/70 flex items-center justify-center font-bold tracking-wider"
          >
            VX
          </div>
          <div class="leading-tight">
            <div class="text-sm font-semibold tracking-[0.25em]">
              VXGEAR
            </div>
            <div class="text-xs text-slate-400 uppercase">
              virtual xperience gear
            </div>
          </div>
        </div>

        <p class="hidden sm:block text-xs text-slate-400">
          Feel the road. <span class="text-[#B79A29]">Virtually.</span>
        </p>
      </header>

      <!-- GRID principal -->
      <section
        class="grid grid-cols-1 lg:grid-cols-[1.2fr_1fr] gap-16 lg:gap-24 items-center"
      >

        <!-- IZQUIERDA -->
        <div class="space-y-6">

          <!-- Badge -->
          <div
            class="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[0.06] px-3 py-1 text-xs text-slate-300 backdrop-blur"
          >
            <span class="h-1.5 w-1.5 rounded-full bg-emerald-400"></span>
            <span class="uppercase tracking-[0.2em] text-[0.65rem]">
              early access • 2026
            </span>
          </div>

          <div class="space-y-4">
            <h1 class="text-4xl sm:text-5xl font-semibold leading-tight">
              The smartest way to
              <span class="block text-[#B79A29]">choose your motorcycle gear.</span>
            </h1>

            <p class="text-base text-slate-300 max-w-xl">
              VXGear is an
              <span class="font-medium text-white">AI-powered gear assistant</span>
              that learns your style, your bike, your weather and the roads you ride — and recommends jackets, helmets, gloves and more that truly fit you.
            </p>
          </div>

          <!-- Avatares estilo Waita -->
          <div class="flex items-center gap-4">
            <div class="flex -space-x-3">
              <div
                class="h-9 w-9 rounded-full border border-slate-900 bg-gradient-to-br from-[#B79A29] to-amber-700 flex items-center justify-center text-sm font-semibold"
              >
                AI
              </div>
              <div
                class="h-9 w-9 rounded-full border border-slate-900 bg-slate-600 flex items-center justify-center text-sm font-semibold"
              >
                VX
              </div>
              <div
                class="h-9 w-9 rounded-full border border-slate-900 bg-slate-500 flex items-center justify-center text-sm"
              >
                🏍
              </div>
            </div>
            <p class="text-sm text-slate-400">
              Riders from <span class="text-white">15+ cities</span> already joined.
            </p>
          </div>
        </div>

        <!-- DERECHA — CARD estilo Waita -->
        <div class="relative">

          <!-- Glow -->
          <div
            class="absolute inset-0 rounded-[28px] bg-[radial-gradient(circle_at_top,_#B79A29_0,_#020617_70%)] opacity-60 blur-2xl"
          ></div>

          <!-- Card -->
          <div
            class="relative rounded-[28px] border border-white/10 bg-white/[0.06] backdrop-blur-xl p-8 shadow-[0_20px_60px_-10px_rgba(0,0,0,0.6)] flex flex-col gap-6"
          >
            <div class="space-y-1">
              <h2 class="text-xl font-semibold text-white">Join the VXGear waitlist</h2>
              <p class="text-sm text-slate-300">
                Be among the first to try the
                <span class="font-medium text-white">AI gear finder</span>.
              </p>
            </div>

            <!-- Form -->
            <form class="space-y-4">
              <div class="space-y-2">
                <label
                  class="block text-xs uppercase tracking-[0.20em] text-slate-300"
                >
                  Email address
                </label>
                <input
                  type="email"
                  required
                  placeholder="you@roadlover.com"
                  class="w-full rounded-xl border border-white/10 bg-white/[0.04] px-4 py-2.5 text-sm text-white outline-none focus:border-[#B79A29] focus:ring-2 focus:ring-[#B79A29]/30"
                />
              </div>

              <button
                type="submit"
                class="w-full rounded-xl bg-[#B79A29] text-slate-900 font-semibold px-4 py-2.5 text-sm transition hover:bg-[#d8b643] shadow-[0_4px_20px_rgba(183,154,41,0.45)]"
              >
                Join waitlist
              </button>
            </form>

            <p class="text-xs text-slate-400">
              Zero spam. Just one email when we launch.
            </p>

            <ul class="text-xs text-slate-300 space-y-1">
              <li class="flex gap-2">
                <span class="h-1.5 w-1.5 rounded-full bg-[#B79A29] mt-1"></span>
                Early access to beta features.
              </li>
              <li class="flex gap-2">
                <span class="h-1.5 w-1.5 rounded-full bg-[#B79A29] mt-1"></span>
                Personalized gear recommendations.
              </li>
              <li class="flex gap-2">
                <span class="h-1.5 w-1.5 rounded-full bg-[#B79A29] mt-1"></span>
                Optional behind-the-scenes updates.
              </li>
            </ul>
          </div>
        </div>
      </section>

      <!-- FOOTER -->
      <footer
        class="mt-16 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-6 text-xs text-slate-500"
      >
        <p>© {year} VXGear — Feel the road. Virtually.</p>
        <p class="text-slate-600">Built with Astro & Tailwind. Crafted for riders.</p>
      </footer>

    </div>

  </main>

</Base>
